#include <stdlib.h>
int main(void)
{
	int no ;
	printf("enter random number\n");
	scanf("%d", &n);
	if(n>0)
	{
		printf(("number is positive");
	}
	if(no<0)
        {
         	 printf("n is negative");
	}
	if(no==0)
	{
	         printf("number is zero")
		 }
}
