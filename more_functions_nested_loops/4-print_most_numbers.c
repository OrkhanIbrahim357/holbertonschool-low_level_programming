#include "main.h"

/**
 * print_most_numbers
 *
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	for (eded = '0'; eded < = = '9'; eded++)

	if (eded != '2' && eded != '4')
		_putchar(eded);
	_putchar('\n');
}
